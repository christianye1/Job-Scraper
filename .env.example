# Copy to ".env" in this directory (project root) and fill in your real key.
# Never commit .env — it is listed in .gitignore.
# Do not put real keys in this file; .env.example may be committed to git.

GEMINI_API_KEY=
